1. string and string method 
    - common escapes \n \t \\ 
    - Raw String r("C:\new\text")
    - Triple qoute String print(""" the first line 
                            the second line""")
2. String Methods

- case_methods
    upper()
    lower()
    capitalize()
    title()

- whitespace_cleaning
    text.strip()
    text.lstrip()
    text.rstrip()    

- splittin_and_joining
    "+".join(words) takes a seperator and insert it into an iterable object 
    sep.join(iterable)

- searching
    text.find("a")
    text.startswith("hello")
    text.endswith("word")